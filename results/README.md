# Results Directory Guide

- `train_logs/`: epoch-level CSV logs and config snapshots
- `eval_logs/`: exported metrics, confusion matrices, and prediction CSV files
- `figures/`: manually saved plots, qualitative visualizations, and presentation figures

Keep text logs and exported metrics here so they can be submitted together with the report.
